@{
    ViewData["Title"] = "Login";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <title>Login</title>
</head>
<body>
<div class="container">
    <h2>Login</h2>
    @using (Html.BeginForm("Login", "Login", FormMethod.Post))
    {
        @Html.AntiForgeryToken()

        <div class="form-group">
            <label for="uname"><b>Username</b></label>
            <input type="text" id="uname-input" placeholder="Enter Username" name="uname" required>
        </div>

        <div class="form-group">
            <label for="psw"><b>Password</b></label>
            <input type="password" id="psw-input" placeholder="Enter Password" name="psw" required>
        </div>

        <button type="submit" id="login-btn" class="btn btn-primary">Login</button>
    }

    @if (ViewBag.ErrorMessage != null)
    {
        <p class="text-danger">@ViewBag.ErrorMessage</p>
    }
    <!-- Add a link to the Register page -->
    <p>Don't have an account? <a href="/Login/Register" id="register-link">Register</a></p>
</div>
</body>
</html>